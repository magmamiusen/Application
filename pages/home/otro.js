import GetType from '../../components/database/GetType';

const Pasteleria = () => {
    return (
        <div>
            <GetType type="Otro" />
        </div>
    );
};

export default Pasteleria;